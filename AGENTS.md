# AGENTS.md

## 言語

- 会話、ドキュメント、コードコメント、エラー説明は日本語で記述する。

## コーディング体制

- コーディングを伴うセッションでは、設計と進行管理を主担当セッションで行う。
- 設計後の実装は、5.6-Terra（high）の別セッションへ委譲する。
- 実装後のコードレビューは、5.6-Sol（low）の別セッションが1系統で行う。
- 主担当は、委譲前に既存の仕様、設計、ハンドオフ、対象コードを読み、受入条件を作業票へまとめる。

## コンテキストと使用量の管理

長期の実装では、品質を落とさずに不要なコンテキスト処理とレビュー往復を減らす。

1. **原則として1セッション1段階とする。**
   設計書の実装段階または独立して検証可能な単位ごとに、実装、レビュー、テスト、コミット、push、CI確認まで終えて区切る。
   次段階はハンドオフを正本として、新しい最小コンテキストで開始する。
2. **サブエージェントへ会話全履歴を渡さない。**
   原則`fork_turns="none"`または必要最小限の直近ターンを使い、読むべき文書の節、対象ファイル、確定仕様、禁止事項、受入条件を短い作業票として渡す。
   過去の会話を理解することに依存した指示を避ける。
3. **実装前に受入条件をチェックリスト化する。**
   設計書の正常系、失敗系、境界条件、永続化、排他、セキュリティ、回帰条件を抽出し、初回のTerra指示へまとめて含める。
   設計書に明記済みの条件をレビュー後に小出しで追加しない。
4. **Terraへの再委譲は指摘を集約して行う。**
   目安は初回実装と、レビュー指摘をまとめた修正の最大2回とする。
   追加修正が必要な場合も、判明した指摘を一度整理してから一括で渡す。短い修正ごとに高推論セッションを起動しない。
   ただし、安全性または正しさの問題を回数制限のために放置してはならない。
5. **Solレビュー前に主担当が機械的な自己監査を行う。**
   受入条件との対応、`git diff --check`、対象テスト、差分範囲、認証情報や個人情報の混入を先に確認する。
   Solレビューは原則1回とし、重大・中程度の機能指摘を修正した場合に再レビューする。
6. **テスト実行を段階化する。**
   実装中は対象テスト、修正後は関連回帰テスト、コミット直前に全単体テストを1回実行する。
   同一差分に対する全テストの反復実行を避ける。CIはpush後に1回確認する。
7. **スレッド上限時に無関係なセッションを再利用しない。**
   過去の別タスクを抱えたエージェントへ新しい実装を割り当てない。枠が不足した場合は、安全な区切りでハンドオフ、コミット、pushを行い、次のセッションへ送る。
8. **途中報告は節目だけにする。**
   ユーザーが詳細を求めていない場合、着手、重要な方針変更またはブロッカー、完了の節目だけを簡潔に報告する。
   待機、CIポーリング、同じテストの経過を逐次説明しない。
9. **使用量節約を品質より優先しない。**
   書き込み安全性、実サイト通信、認証情報、データ破壊、POST後成否不明に関する検証と独立レビューは省略しない。
   節約対象は重複した履歴処理、不要な高推論セッション、重複テスト、冗長な報告とする。

## ハンドオフ

- 各段階の完了時に、現在のコミット、完了範囲、テスト件数、CI結果、未検証事項、次段階の最初の作業を`docs/handoff.md`へ記録する。
- ハンドオフは、会話履歴を渡されていない別セッションが読んでも再開できる内容にする。
- 実測結果、推定、未検証事項を区別し、ライブ検証結果には対象コミットを対応付ける。
